# Build notes: conservative exchange preserves repeated production in autocatalytic reactor networks

Source files in this folder:

- `main.tex` — the paper (article class, amsthm, natbib + hyperref, one TikZ
  module/graph figure drawn inline, two included PDF figures, five tables, one
  `longtable` claims-to-declarations map in Appendix A, one `verbatim`
  reproduction command). Author, affiliation and date are the `\newcommand`
  macros at the top (`\PaperAuthor`, `\PaperAffiliation`, `\PaperDate`);
  author and affiliation are deliberately blank in this version.
  `\CompanionAuthor` (default `Anonymous`) is the author printed for the three
  companion manuscripts in the bibliography (`thermo2026` = C0,
  `recovery2026` = C1, `finiteharvest2026` = C2); set it to the author list
  at the same time. `\RepositoryNote` is the sentence under "Data and code
  availability". Long Lean identifiers are typeset with the breakable
  `\lean{...}` command (a `url`-style command); do not use it in captions,
  section titles or math.
- `refs.bib` — bibliography (`plainnat`, numeric, 28 entries, all cited, set
  in `\small`). All DOIs of the entries not already used by the companion
  papers (Hordijk–Steel 2012, Pierre 2010, Hirsch–Smith 2005, Walter 1970,
  Jacquez–Simon 1993, Semenov et al. 2015, Baez–Pollard 2017,
  Chueh–Conley–Smoller 1977) were resolved through the Crossref API on
  16 September 2026 and match. Crossref records the Hirsch–Smith handbook
  chapter with year 2006; the volume itself is dated 2005 and that is what
  is printed.
- `main.bbl` — generated by BibTeX; arXiv needs it alongside `main.tex`.
- `figures/recovery.pdf`, `figures/accounts.pdf` — produced by
  `make_figures.py` from `figures/trajectory_data.npz` and
  `figures/figure_data.json`, both copied unchanged from the campaign
  workspace (SciPy Radau, rtol 1e-9, atol 1e-12, no clipping; four runs:
  uncoupled, path, ring, star). `figures/diagnostic.py` is the pilot script
  itself, copied for provenance (its SHA-256 is recorded in
  `figure_data.json`). No ODE is integrated by the figure script.
- `check_paper.py` — independent exact replay (run with the repository
  `.venv` Python; needs sympy, mpmath, numpy, scipy). Re-derives the material,
  inventory and weighted-drift identities, stoichiometric annihilation,
  elemental balance and the common thermochemistry, all coefficient
  comparisons, the guarded-growth slack as a nonnegative-coefficient
  polynomial, the pulse image, every Taylor certificate (e^3, e^{36/5},
  e^{3/2}, e^2 upper, exp(6/7) upper with the Lean rounding 2357/1000), the
  four phase rows and their bounds, both backward-weight certificates
  (nilpotent p_A with p_A' = p_A K_A; degree-4 p_B with p_B K_B − p_B' =
  a^4·(0, 3125/6, 2206625/3, 2086306/3), the local identity used in Lean,
  the coefficient ratios at h = 1/24 and h = 1/28, the floors 1/209 and
  1/160), the informational exact-exponential optimum (≈1/157), service and
  food allowances, synthesis thresholds, endpoint rates, handling allowances,
  the dimensioned four-node numbers, the worked mission sizing, the exact
  selective-exchange witness, and every entry of the pilot table from the
  saved JSON. Writes `check_paper_output.json`.
  Last run (16 September 2026): 101 checks, ALL CHECKS PASSED.
- `build.ps1` — full build chain (runs `check_paper.py` and
  `make_figures.py`, builds in a scratch directory under `%TEMP%`, copies
  `main.pdf`, `main.bbl`, `main.log` back here and the PDF to the parent
  folder).

Compiled output is copied to
`..\Conservative_Exchange_Preserves_Repeated_Production_Autocatalytic_Reactor_Networks.pdf`
(25 pages, 16 September 2026). The last MiKTeX build had no errors, no
undefined references or citations, and one 0.2 pt overfull paragraph; every
page was rendered (`pdftoppm`) and inspected.

## Compiling (MiKTeX, user scope, not on PATH)

```powershell
$env:Path = "$env:LOCALAPPDATA\Programs\MiKTeX\miktex\bin\x64;$env:Path"
..\..\..\.venv\Scripts\python.exe check_paper.py
..\..\..\.venv\Scripts\python.exe make_figures.py
pdflatex -interaction=nonstopmode main.tex
bibtex main
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

Build in a fresh scratch directory (as `build.ps1` does) if stale auxiliary
files or the root-log sharing lock cause trouble. Alternative single-pass
engine: the Tectonic binary bundled with the Codex LaTeX plugin
(`E:\CodexData\home\.tmp\bundled-marketplaces\openai-bundled\plugins\latex\bin\tectonic.exe`):

```powershell
& "<tectonic.exe>" -X compile --keep-logs --outdir . main.tex
```

Page previews for visual QA: `pdftoppm -r 60 -png main.pdf render/p`.

Pitfall met while writing: a Bash heredoc in the agent harness collapses a
line-final `\\` (LaTeX row separator) to a single backslash; patch `.tex`
files through a script written with the Write tool instead.

## Sources of the mathematical content

Campaign workspace: `problem_workspaces\RAF_C4_composition_on_defined_assemblies`
(`PUBLICATION_HANDOFF.md`, `HANDOFF.md`, `THEOREM.md`, `CLAIM_MAP.md`,
`HYPOTHESES.md`, `DIMENSIONED_EXAMPLE.md`, `SPRINTS\2026-09-15.md`,
`C4_Conservative_Exchange_Paper.tex/.pdf` — the 14-page workspace
manuscript of 16 September that was supplied as the plan, together with the
Markdown supplement `C4_Conservative_Exchange_Supplementary_Information.md`
whose revision list S15.1 this version implements — and the strict
receipts). Every constant was re-derived here (`check_paper.py`).

Lean sources (project root `mathlib4_project\`, toolchain
`leanprover/lean4:v4.30.0`, frozen manifest SHA-256 `a8df0b60…`), all under
`proofs\C4Assemblies\`: `Transport`, `Comparison`, `Bounds`, `Source`,
`Family`, `LiteralSource`, `Thermochemistry`, `GlobalFlow`, `Uniqueness`,
`Material`, `StockRecovery`, `Recovery`, `Return`, `PhaseFaces`,
`PhaseTransfer`, `FreeProduct`, `OutputSupplies`, `Repeated`,
`SharedAccounts`, `BudgetSafety`, `Cutoff`, `Inventory`, `Synthesis`,
`SupplyCeiling`, `Resolution`, `Examples` (baseline root
`conservative_assembly_operation`, floor 1/540, receipt
`Examples.verify.json`), `SharpPhaseBounds`, `SharpPhaseTransfer`,
`SharpFreeProduct`, `ReadySynthesis`, `Publication` (floor 1/209, receipt
`Publication.verify.json`, source SHA-256 `b53986f7…`), and the four modules
added for this paper on 16 September 2026: `FinePhaseBounds`,
`FinePhaseTransfer`, `FineFreeProduct`, `PublicationFine` (root
`fine_conservative_assembly_operation`, floor 1/160, receipt
`PublicationFine.verify.json`, source SHA-256 `ad7da3c0…`, four exported
declarations, axioms propext/Classical.choice/Quot.sound only). Local
kinetics come from `proofs\ProductiveRecovery\` (the C1 development) and the
coefficient binding from `proofs\CommonPhysicalRealization\`.

What this version adds beyond the 14-page workspace manuscript:

- The sharper free-template floor 1/160 (distinct phase losses 48, 43, 41,
  24 with the duplex-to-complex link, degree-four backward weight at lag
  1/28, Taylor bound on exp(6/7)); Lean-verified, not only conventional.
  Every downstream number (Nm/160, 1/640, 160 D_X/N, 25 nmol, 1/40 µmol per
  four-node cycle, 11/40 µmol in the worked sizing) was updated accordingly.
- A literature introduction (RAF theory, serial transfer, linked reactor
  modules, compositional CRN, comparison/monotone systems, compartmental
  transport, open-network thermodynamics, companion papers), contributions
  list, three-kinds-of-evidence paragraph, non-claims and organization.
- Full written proofs in the order of the Lean chain: the three transport
  properties, the finite comparison principle (strict and transport forms),
  the cutoff/positive-part existence construction and Grönwall uniqueness,
  pulse image, material contraction, guarded growth, the fence, return, the
  phase lower system as a Metzler matrix, the general backward-weight lemma,
  both certificates with the derivative-cancellation and two-step-path
  explanations requested by the supplement, the inventory telescope and net
  synthesis.
- Remark 6.4 (where the slack is), the dilution derivation (S10.1), the
  unequal-volume remark (S13.2), the open-question list, the copyable
  reproduction command, and full bibliographic entries replacing the
  workspace-label references.
- A TikZ module/graph figure; a three-panel trajectory figure adding the
  node-minimum free X of all four runs; a four-panel accounts figure with the
  service allowance on its own axis (S15.2).

## What is Lean-verified and what is conventional

Lean: the literal coupled field and transfer marks, elemental/potential
balance of transfers, the three transport properties, the finite comparison
principle, global nonnegative trajectories and uniqueness, the pulse image,
material contraction and the corridor, guarded growth at a minimizing node,
the fence, conditioning and routine return, the phase inequalities (common
loss 48 and distinct losses), both backward-weight certificates and the floors
1/209 and 1/160, template inventory bounds, food and service budgets, one
segment, arbitrary history-dependent repetition, the inventory telescope and
both net-synthesis statements, the assembled root theorem (Theorem 3.1
(a)–(d) as one declaration in three versions), the closed path and ring
instances, the prefix/cutoff agreement, the transport (handling) allowance
and the one-segment food ceiling.

Conventional (proved in the text): mission sizing (28), endpoint rates (30),
edge/degree scaling of the handling allowance, the multi-segment material
ceiling, dimensional conversions, the dilution derivation (Section 10.2),
the unequal-volume remark (Section 10.3), the certificate-validation count.

Exact arithmetic, not Lean: the selective-exchange witness (Proposition
10.1), replayed by `check_paper.py`.

Numerical (diagnostic, not proof): Table 4 and Figures 2–3.

## Excluded, available for a supplemental report

The 49-item baseline and 24-item publication task registers and the sprint
ledger; AGC checkpoints, frontier bindings and the `post-proof-publish`
history; the compiler-diagnostic history (Fin-numeral normalization, binder
scope in finite sums, unreduced lambda in a fence rewrite) recorded in
`HANDOFF.md`; the full declaration-interface hashes and dependency lists of
the receipts; the supplement's development-history table (S11.2) and its
extension proposals beyond the unequal-volume remark (S13.1 matrix search
is now superseded by the compiled 1/160 certificate; S13.3–S13.4 remain
open); the publication snapshot zip and manifest.

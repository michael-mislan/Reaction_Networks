# Build notes: MinRAF inapproximability paper

Source files in this folder:

- `main.tex` — the paper (article class, amsthm, hyperref). Author, affiliation
  and date are the three `\newcommand` macros at the top of the file.
- `refs.bib` — bibliography (plain style).
- `main.bbl` — generated by BibTeX; arXiv needs it alongside `main.tex`.

Compiled output is copied to `..\MinRAF_Inapproximability.pdf`.

## Compiling (MiKTeX, user scope, not on PATH)

```powershell
$env:Path = "$env:LOCALAPPDATA\Programs\MiKTeX\miktex\bin\x64;$env:Path"
pdflatex -interaction=nonstopmode main.tex
bibtex main
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

Alternative single-pass engine (Tectonic bundled with the Codex LaTeX plugin):

```powershell
& "$env:USERPROFILE\.codex\.tmp\bundled-marketplaces\openai-bundled\plugins\latex\bin\tectonic.exe" -X compile --keep-logs --outdir . main.tex
```

## arXiv submission

Upload `main.tex`, `refs.bib` and `main.bbl` (arXiv runs pdflatex itself and
uses the `.bbl`). No figures or custom style files are required.

## Provenance

The mathematical content is drawn from the Lean development in
`E:\Erdos Problems\proofs\MinRAFApprox` and the workspace
`E:\Erdos Problems\problem_workspaces\RAF_minRAF_const_factor_approx`
(handoff, publication audit, verification receipts, numerical oracle).
Hashes quoted in Section 10 of the paper come from
`verification/PublicationResolution.verify.json` and
`verification/Resolution.verify.json`; the axiom lists come from the
compiler logs referenced in those receipts.

# Build notes: Critical-window emergence of autocatalytic sets under reaction-channel quotienting

Source files in this folder:

- `main.tex` — the paper (article class, amsthm, TikZ, hyperref). Author,
  affiliation and date are the three `\newcommand` macros at the top of the
  file (author and affiliation are currently blank, as requested).
- `refs.bib` — bibliography (plain style). The two companion manuscripts
  (`criticalwindow2026`, `overlapgateway2026`) carry the placeholder author
  `[Author name to be inserted]`; fill these in before submission.
- `main.bbl` — generated by BibTeX; arXiv needs it alongside `main.tex`.
- `make_figures.py` — produces `data/census.json`, `data/table_rows.tex`
  (Table 1) and `figures/finite_size.pdf` (Figure 2). Exact enumeration for
  n <= 12 (reproduces the workspace pilot census), closed-form
  Lyndon–Schützenberger count for n <= 40, and exact evaluation of the
  full-pool openness 1-(1-p_n)^{M_n}. No Monte Carlo anywhere.
  Figure 1 is inline TikZ.

Compiled output is copied to
`..\Critical_Window_Reaction_Channel_Quotienting.pdf`.

## Compiling (MiKTeX, user scope, not on PATH)

```powershell
$env:Path = "$env:LOCALAPPDATA\Programs\MiKTeX\miktex\bin\x64;$env:Path"
& 'E:\Erdos Problems\.venv\Scripts\python.exe' make_figures.py
pdflatex -interaction=nonstopmode -halt-on-error main.tex
bibtex main
pdflatex -interaction=nonstopmode -halt-on-error main.tex
pdflatex -interaction=nonstopmode -halt-on-error main.tex
```

Alternative single-pass engine (Tectonic bundled with the Codex LaTeX plugin):

```powershell
& "$env:USERPROFILE\.codex\.tmp\bundled-marketplaces\openai-bundled\plugins\latex\bin\tectonic.exe" -X compile --keep-logs --outdir . main.tex
```

The final build has zero overfull boxes and no undefined citations or
references (check `main.log`). MiKTeX's "check for updates" notice is not a
manuscript error.

## arXiv submission

Upload `main.tex`, `refs.bib`, `main.bbl`, `data/table_rows.tex` and
`figures/finite_size.pdf` (arXiv runs pdflatex itself and uses the `.bbl`).
Before submitting: set the author macros, replace the placeholder authors of
the two companion entries in `refs.bib`, and confirm that
`https://github.com/mmislan/Erdos` is public (it is cited in the data
availability paragraph and Appendix A).

## Provenance and claim boundary

The mathematics is drawn from the Lean development in
`E:\Erdos Problems\proofs\RAFReactionQuotient` (32 modules, root
`Resolution.lean`, strict receipt
`problem_workspaces\RAF_critical_window_reaction_quotient\verification\Resolution.verify.json`,
freshness audit `results\resolution_audit.json`) and the workspace
`problem_workspaces\RAF_critical_window_reaction_quotient` (FINAL_HANDOFF.md,
RESEARCH_ACCOUNT.md, POST_ROOT_WORK.md, publication/FOLLOWUP_RESULTS.md,
publication/paper.tex — the earlier 11-page critical-path draft that this
paper supersedes, experiments/quotient_pilot_results.json).

Hashes quoted in Appendix A (root source `ca3264cf…`, receipt `0f091921…`,
lake manifest `a8df0b60…`) are taken from `results/resolution_audit.json`
and `publication/manifest.json` as of 2026-09-14; the repository HEAD at
build time was `c77fef935441d41c664f04c9deb6c53504a7049f`.

### Lean-verified vs conventional

Lean-verified (compiled, warnings as errors, axioms propext / Classical.choice
/ Quot.sound only): Theorem 2.2 and every lemma/proposition of Sections 3–8
(each carries a `Lean:` line naming its declarations); Proposition 9.1
(monotonicity) and Proposition 9.2 (sharp lower comparison) via
`PublicationConsequences.lean`; the first cap in Proposition 9.4 is the
combination of two compiled statements; Corollary 9.6(i) is the pre-existing
sublinear theorem of the OverlapCorrectedRAF development.

Conventional (hand-proved, marked "Conventional proof; not formalised." in the
text): Proposition 9.3 (upper comparison S_qt <= S_sp), the productive-gateway
bound S_qt(a) <= 1-(1-a)^30 in Proposition 9.4, Proposition 9.5 (endpoint
values S_qt(0)=0, S_qt(1)=1 and the two limits of Theta_qt), Corollary 9.6(ii)
(superlinear regime), Corollary 9.7 (heterogeneous envelopes). The strictness
question Theta_qt < Theta_sp is stated as open; the conventional trap argument
in RESEARCH_ACCOUNT.md is described but not claimed.

### Numbers

Example 3.2 fractions (4077/4096, 239/256, 505585/531441, 5137/6561) come
from `experiments/quotient_pilot_results.json` (exact rational enumeration).
Table 1 and Figure 2 are regenerated by `make_figures.py`. The supplied-seed
cutoffs in Remark 4.3 (k = 53667, L = 536670 at a0 = 0.1; k = 2082, L = 20820
at a0 = 0.5, both for m = 10) are the values recorded in
`publication/worked_example.json` and recompute from (10) directly.

### Not included (candidate supplementary reports)

The discovery history and failed routes (RESEARCH_ACCOUNT.md,
CYCLE_LEDGER.jsonl, daily/2026-09-14.md), the full strict-trap coupling
argument (POST_ROOT_WORK.md), and the complete per-module receipt inventory.

# Build notes: collective thermodynamic incompatibility of autocatalytic cores

Source files in this folder:

- `main.tex` — the paper (article class, amsthm, TikZ figures, listings for
  Lean snippets, natbib + hyperref). Author, affiliation and date are the
  three `\newcommand` macros at the top of the file.
- `refs.bib` — bibliography (`unsrtnat` style, numeric, citation order).
- `main.bbl` — generated by BibTeX; arXiv needs it alongside `main.tex`.
- `check_paper.py` — exact rational replay of every explicit number printed
  in the paper (standard library only). Run `py check_paper.py`; it prints
  `PASS`. It guards transcription; it is not a proof of any universal claim.

Compiled output is copied to
`..\Collective_Thermodynamic_Incompatibility_Autocatalytic_Cores.pdf`.

All figures are drawn in TikZ inside `main.tex`; there are no external
graphics files. The earlier ReportLab-rendered nine-page draft and its vector
figures remain in
`problem_workspaces\RAF_thermo_compatibility_hypergraph\publication\` and are
not used here.

## Compiling (MiKTeX, user scope, not on PATH)

```powershell
$env:Path = "$env:LOCALAPPDATA\Programs\MiKTeX\miktex\bin\x64;$env:Path"
pdflatex -interaction=nonstopmode main.tex
bibtex main
pdflatex -interaction=nonstopmode main.tex
pdflatex -interaction=nonstopmode main.tex
```

Alternative single-pass engine (Tectonic bundled with the Codex LaTeX plugin):

```powershell
& "$env:USERPROFILE\.codex\.tmp\bundled-marketplaces\openai-bundled\plugins\latex\bin\tectonic.exe" -X compile --keep-logs --outdir . main.tex
```

Page previews for visual QA can be rendered with PyMuPDF (`py -c "import fitz"`)
or with the Poppler bundled in the Codex runtime:

```powershell
& "$env:USERPROFILE\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\poppler\Library\bin\pdftoppm.exe" -png -r 80 main.pdf preview\p
```

## Sources of the mathematical content

- Lean development: `proofs\ThermoCoreCompatibility\Hypergraph\` (Lean 4.30.0,
  frozen Mathlib manifest). The strict root is
  `ThermoCoreCompatibility.Hypergraph.reviewed_resolution` in `Reviewed.lean`;
  the original package `tcch_resolution` is in `Main.lean`.
- Workspace and campaign record:
  `problem_workspaces\RAF_thermo_compatibility_hypergraph\` (final handoff,
  review handoff, research ledger, scorecard, exact scripts, receipts,
  `publication\theorem_concordance.md`, `publication\data\evidence_manifest.json`).
- Receipt hashes quoted in the paper's verification section come from
  `root_receipt.json`, `reviewed_receipt.json` and
  `publication\data\evidence_manifest.json` in that workspace.
- Predecessor paper: `key_results\RAFs\Thermo_Core_Compatibility_arxiv\`.

## arXiv submission

Upload `main.tex`, `refs.bib` and `main.bbl` (arXiv runs pdflatex itself and
will use the `.bbl`). Set the author macros first. Suggested categories:
q-bio.MN (primary), math.OC or math.CO (cross-list), cs.LO for the formal
verification component.

## Items to settle before submission

- Author list, affiliation, acknowledgements.
- The predecessor's arXiv identifier (bib key `tcic2026`) once it is posted.
- A public release tag and DOI-bearing archive of the Lean sources, so that
  the receipt hashes in the verification section can be checked against a
  public artifact.
- Re-run the bounded novelty audit (the one cited is dated 31 August 2026).

# P012 publication source and declaration review

The final handoff explicitly identifies `RankOneDock.lean` and
`LengthBarrier.lean` as unused exploratory drafts, not terminal dependencies or
verified deliverables. `RankOneDock` failed fresh strict compilation with an
unknown `hne` identifier. That failure is preserved as a diagnostic; the draft
was not repaired and is not counted as verified.

The selected manuscript's BUILD and theorem index identify `PublicationAudit`
as the publication target. Its original strict receipt authenticates the root
and all 52 dependencies, exactly matching the computed 53-module closure and
every retained source hash. The separate `PolynomialIntensityAudit` receipt also
matches its complete closure. The final selection contains those 54 distinct
modules, preserving the adjacent scalar supplement included in the original
freeze inventory.

The audit now covers all twelve publication declarations, plus the supplement's
two scalar input lemmas, with exact elaborated types from the authenticated
receipts. Those two lemmas remain outside the paper and do not establish a
growing-intensity RAF theorem. The manuscript's cited positive quadratic bound
also remains an external result, not a local formalization claim.

No retained proof bytes, hypotheses, or publication declarations changed. The
original Erdos files are untouched. Destination copies of the two drafts and
the failed compiler log remain in the ignored migration archive.

Evidence: [preserved final handoff](../../papers/P012-sparse-model-no-linear-rafs/evidence/source-maps/03-FINAL_HANDOFF_REPORT.md),
[publication theorem index](../../papers/P012-sparse-model-no-linear-rafs/evidence/source-maps/02-THEOREM_INDEX.md),
and [machine-readable review with receipt hashes](P012.json).

# P030 publication source review

The preserved publication receipts identify `Remark34.lean` and
`PublicationFollowups.lean` as final targets. The initial selection omitted these
targets and two dependencies. All four files are now copied byte for byte from
the original repository; receipt hashes and complete dependency sets were checked.

The original joint-probability declaration remains mapped. The audit now also
covers five declarations requested by the publication receipts and two structural
limit declarations from the retained `PowerLawSmallRAF.Main` root. This expands
the audit from one declaration to eight without rewriting any proof.

See [the machine-readable review](P030.json) for file hashes and provenance.
Historical verification is provenance; fresh compilation and audit remain required.

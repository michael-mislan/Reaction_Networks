# P040 publication source review

The preserved build instructions select `BimolecularClassification.lean` and
`BimolecularCorollaries.lean`. The classification's original strict receipt
authenticates its root and all 1,072 dependencies. This exact set matches the
computed import closure; the cited corollary adds one module, giving 1,074.

The initial folder-wide selection also included 951 experimental or unused files
outside this published chain. Their destination copies were retained in the
ignored migration archive. The original Erdos files remain untouched. No retained
source bytes, classification domain, necessary certificate, or premise changed.

The audit retains both classification declarations and adds all seven named
theorems in the cited corollary module. The historical classification audit reports
3,826 native-evaluation axioms plus the standard three; fresh auditing must record
the actual axioms for all nine declarations. The finite classification and the
manuscript's conventional analytic arguments retain their distinct scopes.

See [the machine-readable review](P040.json) for the exact source set and hashes.
